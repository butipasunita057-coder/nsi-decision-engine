# Echofy · 项目规格说明（SPEC.md）

> 本文档是 Echofy（项目对外展示名；MVP 内部代号：NSI 决策引擎）的产品 + 技术规格说明，用于指导黑客松现场开发。
> 文档面向比赛评委、队员、以及后续在现场执行代码的 AI 编程助手（Claude Code / Codex / Qoder）。
> 本文档与 PLAN.md、TASKS.md 配套使用。

---

## 1. 项目定位

**项目名称**：Echofy（对外展示名）。MVP 内部代号仍为 NSI 决策引擎，仅出现于技术文档与代码。

**公开指标名**：Echo Score（面向评委与卖家的评论健康度分数，内部计算函数仍为 `calc_nsi`）。

**一句话定位**：
输入一个亚马逊竞品 ASIN，输出一份红 / 黄 / 绿三色排序的"改品清单"，告诉亚马逊中小卖家：**先改哪个、怎么改**。

**项目类型**：垂类电商决策辅助工具（AI + 电商方向）。

**形态**：本地运行的 Streamlit 单页应用，仅依赖本地 JSON 数据，无后端服务、无数据库、无登录系统。

**比赛归属**：鹭岛青年筑梦 AI 创新大赛 · 电商黑客松（赛题方向：AI + 电商，从痛点到方案）。

---

## 2. 目标用户

主要面向亚马逊平台的中小卖家，以及围绕这些卖家工作的相关角色：

1. 亚马逊中小卖家老板：精力有限，每天看到大量评论，无法逐条决策。
2. 产品经理 / 产品负责人：需要明确"下一版改什么"。
3. 选品负责人：在选品 + 改品阶段需要快速识别风险点。
4. 运营 / Listing 优化人员：需要明确哪些问题是"页面表达"，哪些是"产品本身"。

不是面向：大型品牌方、平台工具厂商、消费者。

---

## 3. 真实业务痛点

亚马逊中小卖家每天面对大量评论，他们能感受到"产品有问题"，但难以回答下面这些问题：

1. 这么多差评，哪个问题最致命？
2. 我下一版到底先改哪个？
3. 这个差评是我产品本身的问题，还是页面没说清？
4. 这条差评是不是恶意评论，需不需要认真对待？
5. 改完之后，我应该在 Listing 里怎么写，才能避免下一波客户继续踩坑？

现状：
- 卖家通常凭直觉、凭印象、凭最近一条投诉做决定。
- 改了不重要的问题，最致命的问题还在。
- 团队内部经常因为"先改哪一个"反复争论。
- 评论数据散落在评论区、表格、群聊里，没有结构化决策依据。

---

## 4. 核心价值主张

NSI 决策引擎（对外展示名：Echofy）只解决一件事：

> **把"一堆杂乱的评论"，变成"一份按优先级排序的、可执行的改品清单"。**

具体价值：

1. 帮卖家在 30 秒内识别"最致命的产品问题"。
2. 帮卖家区分"产品问题 vs 页面问题 vs 服务问题 vs 恶意评论"，避免错改。
3. 给出每个问题对应的建议动作和 Listing 改写建议。
4. 用红 / 黄 / 绿三色，让非技术老板一眼就能看懂。
5. 决策过程透明：每个结论背后都给出 Echo Score 数值、正负面提及数、典型评论原文。

---

## 5. 黑客松适配说明

黑客松要求"小而真，跑得通，讲得清"。本项目主动收敛到一个最小闭环：

- 一个清晰的真实痛点：卖家不知道先改哪个。
- 一个清晰的解法：NSI + 三色排序 + P/L/S/M 归因。
- 一个可现场跑通的 Demo：Streamlit + 本地 JSON。
- 一个可被解释的 AI 链路：评论 → 特征聚合 → NSI → 三色 → 归因 → 建议动作 + Listing 改写。
- 一份可在 5 分钟内讲完的故事：痛点 → 方案 → Demo → 价值。

主动放弃：
- 不做大而全的"亚马逊运营平台"。
- 不做账号系统、数据库、订阅付费。
- 不做实时爬虫、跨境合规、海外仓等无关支线。

---

## 6. 评分规则适配说明

赛事评分维度（共 100 分）及本项目对应策略：

| 评分维度 | 权重 | 本项目对应策略 |
| --- | --- | --- |
| 技术实现 | 40 分 | AI 真正参与核心流程：从评论到三色改品清单的链路完整、稳定可运行；NSI 计算、归因分类、建议生成构成可解释的 AI 决策链。 |
| 创意 / 商业价值 | 30 分 | 切中亚马逊中小卖家"先改哪个"这一具体痛点；以 NSI + 三色 + P/L/S/M 归因形成差异化方法论，明确商业落点（卖家 SaaS 工具雏形）。 |
| 完成度 | 30 分 | 现场完成可运行 Demo、闭环演示路径、清晰路演表达，PPT、GitHub、5 分钟录屏齐全。 |
| 项目真实性 | 一票否决 | 核心代码、核心逻辑、核心创意均现场完成；赛前仅准备 SPEC / PLAN / TASKS 三份文档，不预先实现核心功能。 |

---

## 7. 项目真实性策略

赛事将"项目真实性"列为一票否决项。本项目执行以下纪律：

1. 赛前**只产出文档**：SPEC.md、PLAN.md、TASKS.md。
2. 赛前**不写**任何 Python 源码、不写 app.py、不创建 demo_data.json、不创建 requirements.txt、不创建 README.md。
3. 赛前**不搭建**任何可运行项目骨架。
4. 现场开发严格按 TASKS.md 从 T0 → T18 顺序执行，所有核心代码、核心逻辑、核心创意现场完成。
5. Git 提交记录从现场开始，commit 历史能反映真实开发过程，不做"一次性大提交"。
6. 演示时主动说明：本项目核心代码均在现场完成，赛前仅准备规格文档。
7. 评委如要求展示开发过程，可直接展示 Git 提交时间线 + 文档先于代码的事实。

---

## 8. 技术路线（固定，不可扩展）

唯一允许的技术栈：

- 语言：Python 3.10+
- 前端 / UI：Streamlit
- 数据：本地 JSON 文件（`demo_data.json`）
- 依赖：标准库 + Streamlit 即可，最多再加 1~2 个轻量库（如 `pandas`，仅在确实需要时使用）。

明确禁止引入：
- FastAPI、Flask、Django
- React、Vue、任何前端构建工具
- Docker、docker-compose
- PostgreSQL、MySQL、SQLite、MongoDB、Redis
- Selenium、Playwright、Scrapy 等爬虫框架
- 真实亚马逊 API、PA-API
- LangChain 等大型链式框架
- 鉴权系统（OAuth、JWT 等）

允许但不优先：
- OpenAI / 通义千问等 LLM API：仅在 Listing 改写建议或诊断文案生成等"非关键稳定路径"上可选使用，且必须有"无 API Key 时也能跑通"的本地兜底。

---

## 9. MVP 功能范围（必须做的）

MVP 必须包含且仅包含以下功能：

1. ASIN 输入：支持下拉选择示例 ASIN，也支持手动输入 ASIN 字符串。
2. 一键 Analyze：点击按钮触发分析。
3. 整体健康度评分：基于所有特征 NSI 加权计算的整体得分（0-100）。
4. 三色改品清单：所有特征按 NSI 升序排序（最差在前），分别用红 / 黄 / 绿标识。
5. 单特征卡片：每个特征展示 NSI 数值、正面提及数、负面提及数、总提及数、P/L/S/M 归因标签。
6. 问题详情展开区：每张卡片可展开，展示典型评论、诊断说明、建议动作、Listing 改写建议。
7. 友好错误提示：ASIN 不存在、数据为空、字段缺失时给出明确的中文提示。

---

## 10. 明确不做事项（Out of Scope）

为保证黑客松最小闭环 Demo，以下功能**一律不做**：

- 不做真实亚马逊评论爬虫。
- 不做任何数据库（不用 SQLite、不用任何 RDBMS、不用任何 NoSQL）。
- 不做用户登录、注册、鉴权。
- 不做多租户、不做权限控制。
- 不做完整 SaaS 平台、不做付费订阅。
- 不做用户后台、订单管理、支付。
- 不做长期数据存储 / 历史趋势对比。
- 不做完整报告导出系统（最多支持简单 Markdown 文本展示）。
- 不做浏览器扩展。
- 不做手机 App。
- 不做 FastAPI / Flask 后端。
- 不做 React / Vue 前端。
- 不做 Docker 部署。
- 不做大模型 fine-tune、向量检索、RAG。
- 不做多 ASIN 批量分析（MVP 只支持单 ASIN）。

---

## 11. 数据结构设计

唯一数据源：`demo_data.json`（现场创建，赛前不创建）。

顶层结构：以 ASIN 为 key 的字典。建议结构如下：

```json
{
  "B0EXAMPLE01": {
    "asin": "B0EXAMPLE01",
    "title": "示例蓝牙耳机",
    "category": "Electronics / Headphones",
    "summary": "面向通勤场景的入门级蓝牙耳机，主打长续航和轻量化。",
    "features": [
      {
        "feature_id": "battery_life",
        "feature_name": "续航",
        "positive": 12,
        "negative": 38,
        "total": 60,
        "attribution": "P",
        "diagnosis": "续航虚标严重，多名用户反馈实际不足 4 小时。",
        "action": "重新核对电池容量，更新真实续航参数，避免页面虚标。",
        "listing_rewrite": "实测连续播放约 4 小时，关闭降噪可延长至 5 小时；不再使用'超长续航'类描述。",
        "examples": [
          { "polarity": "negative", "text": "宣传 8 小时，实际 3 小时不到。" },
          { "polarity": "negative", "text": "出门半天就没电了。" }
        ]
      }
    ]
  }
}
```

字段说明：

- `asin`：ASIN 字符串。
- `title` / `category` / `summary`：用于头部展示，可选但推荐有。
- `features[]`：该 ASIN 下所有产品特征的评论汇总。
- `feature_id`：英文蛇形命名，唯一。
- `feature_name`：中文展示名（如"续航"、"密封性"）。
- `positive` / `negative` / `total`：正面、负面、总提及次数（整数）。`total` 不要求等于 `positive + negative`，可以包含中性提及。
- `attribution`：取值必须是 `"P"`、`"L"`、`"S"`、`"M"` 之一。
- `diagnosis`：一句话诊断，给老板看。
- `action`：建议动作。
- `listing_rewrite`：Listing 改写建议（如适用，否则可留空字符串）。
- `examples[]`：典型评论，每条含 `polarity`（`positive` / `negative` / `neutral`）和 `text`。

赛前不创建 demo_data.json；T2 任务在现场创建至少 2 个示例 ASIN，每个 ASIN 至少 6 个特征。

---

## 12. NSI / Echo Score 计算规则

> 对外展示为「Echo Score」；内部代号仍为 NSI，函数名 `calc_nsi` 不变，以保障历史代码与单元测试稳定。
>
> 对外表述（在 README、路演、视频中使用的软化描述）：
> 「Echo Score 基于正负向评论提及差异和总提及量计算，用来衡量某个产品特征的评论健康度。」
>
> 以下为内部技术细节，用于指导代码实现，不作为对外宣传 headline。

NSI = Net Sentiment Index，定义如下：

```
NSI = (positive - negative) / total
```

约束与边界：

1. 当 `total == 0` 时，NSI 视为 `None`，对应特征不参与排序，并在 UI 上单独标注"提及不足"。
2. NSI 取值范围理论上为 `[-1, 1]`，UI 展示保留两位小数。
3. NSI 计算函数必须是纯函数，仅依赖输入参数 `positive`、`negative`、`total`，无副作用。
4. 输入非数字、负数时抛出明确异常或返回 `None`，由调用层统一兜底。

---

## 13. 红 / 黄 / 绿分类规则

唯一权威阈值：

| 颜色 | 条件 | 含义 | 行动建议 |
| --- | --- | --- | --- |
| 🔴 红色 | `NSI < -0.5` | 致命问题 | 立即修复，下一版必须改 |
| 🟡 黄色 | `-0.5 <= NSI < 0` | 潜在问题 | 逐步优化，可排期 |
| 🟢 绿色 | `NSI >= 0` | 核心优势 | 保持或在 Listing 中突出 |

实现要求：

1. 阈值必须集中在一个常量或一个函数中，不允许在 UI 代码里硬编码多份。
2. 边界值归属严格按上表（`-0.5` 归黄色，`0` 归绿色）。
3. 当 NSI 为 `None`（提及不足）时，单独归为"灰色 / 提及不足"，不计入红黄绿统计。

---

## 14. P / L / S / M 归因规则

`attribution` 字段取值固定为以下四种：

| 缩写 | 含义 | 处理方向 | 示例 |
| --- | --- | --- | --- |
| P | Product，产品本身问题 | 改产品 / 改供应链 | 材质差、续航虚标、做工差、易坏 |
| L | Listing，页面表达问题 | 改页面 / 改文案 / 改图 | 图片误导、尺寸没说清、参数不准 |
| S | Service，服务问题 | 改物流 / 改客服 / 改售后 | 物流慢、客服不回、售后差 |
| M | Malicious，恶意或无效评论 | 忽略 / 申诉 | 同行刷差评、与产品无关、纯情绪 |

实现要求：

1. 归因来自 demo_data.json 中预设值（赛中由人工 + AI 共同标注，避免现场训练模型）。
2. UI 上必须用清晰可读的彩色标签或图标展示。
3. 红色卡片中如归因为 M（恶意），必须给出"建议申诉"或"建议忽略"提示，不能误导卖家去改产品。

---

## 15. 页面结构

整个 Demo 是单页 Streamlit 应用，自上而下分为 5 个区块：

1. **顶部标题区**
   - 项目名称：Echofy（对外展示名；MVP 内部代号：NSI 决策引擎）
   - 一句话定位
   - 简短副标题：把评论噪音变成改品优先级

2. **输入区**
   - ASIN 选择下拉框（来自 demo_data.json 中已有 ASIN）
   - ASIN 手动输入框
   - "Analyze" 按钮

3. **概览区（Overview）**
   - 商品标题、类目、简介
   - 整体健康度评分（0-100）+ 文字结论（健康 / 一般 / 危险）
   - 红黄绿三色特征数量统计

4. **三色改品清单（核心区）**
   - 按 NSI 升序排列的特征卡片列表
   - 每张卡片显示：颜色条、特征名、NSI、正/负/总提及、P/L/S/M 标签
   - 卡片可展开

5. **问题详情展开区**
   - 典型评论（正面 + 负面）
   - 诊断说明（diagnosis）
   - 建议动作（action）
   - Listing 改写建议（listing_rewrite）

页面不需要侧边栏复杂选项；保持极简、可演示。

---

## 16. Demo 演示路径

唯一固定演示路径，5 分钟内必须能讲完：

1. 打开 Streamlit 页面。
2. 简述痛点：卖家面对大量评论不知先改哪个。
3. 选择示例 ASIN（推荐选一个"红色致命问题最显眼"的样例）。
4. 点击 Analyze。
5. 解读整体健康度评分。
6. 解读三色改品清单：先看红色，说明这是必须立刻改的。
7. 展开一张红色卡片，朗读典型评论 + 诊断 + 建议动作 + Listing 改写。
8. 切到一张黄色卡片，说明潜在问题。
9. 切到一张绿色卡片，说明这是产品优势，应在 Listing 中突出。
10. 总结：本工具帮中小卖家把"一堆评论"变成"一份排序好的改品清单"。

---

## 17. 验收标准

满足以下全部条件视为 MVP 通过验收：

1. 在赛事现场提供的电脑上，使用 `streamlit run app.py` 一条命令即可启动。
2. 启动后无报错，浏览器能正常打开页面。
3. 选择任一示例 ASIN，点击 Analyze 后 2 秒内出结果。
4. 结果页同时包含：整体健康度评分 + 红黄绿三色清单 + 至少 1 张可展开的卡片详情。
5. 红黄绿分类与 NSI 数值一致，无矛盾。
6. P/L/S/M 标签全部显示，无空标签。
7. 输入不存在的 ASIN 时显示中文友好提示，不抛栈。
8. 数据全部来自本地 JSON，断网也能跑通。
9. 演示全程不超过 5 分钟。
10. GitHub 仓库可访问，README 至少包含项目介绍、运行命令、Demo 截图。

---

## 18. 风险控制

| 风险 | 等级 | 应对策略 |
| --- | --- | --- |
| 现场环境无法安装 Streamlit | 高 | T1 第一步先在现场环境跑通 `streamlit hello`，确认依赖可用。 |
| demo_data.json 数据过假，被评委质疑 | 中 | 数据基于真实电商常见特征（续航、密封、物流等）构造，并在 PPT / 演示中说明这是"演示用样例数据"。 |
| 现场时间不够，UI 不完整 | 高 | 严格按 TASKS.md 顺序，T1~T8 是必做 MVP，T9~T11 是体验加分，T12~T18 是收尾，先保证主路径再做美化。 |
| 评委质疑项目真实性 | 极高（一票否决） | 严格执行第 7 节的真实性策略：赛前只交付文档，现场提交时间线清晰可查。 |
| Streamlit 缓存导致数据不刷新 | 低 | 使用 `@st.cache_data` 时仅缓存 JSON 加载；切换 ASIN 时必须重新计算。 |
| ASIN 输入错误导致崩溃 | 中 | T12 任务专门处理友好错误提示，不让任何异常直接抛到 UI。 |
| 代码集中在一两次大 commit，被怀疑预先开发 | 高 | 按 TASKS 任务粒度提交，每个 T 任务至少一个 commit，commit message 写明对应任务编号。 |
| 临时引入复杂依赖（爬虫、数据库等）拖时间 | 高 | 严格遵守第 8、第 10 节的禁止清单，禁止在比赛中临时切换技术栈。 |
| 评分老师看不懂 NSI 公式 | 中 | PPT 必须用一页讲清 NSI 公式 + 红黄绿阈值，演示时口头复述一次。 |
| 录屏卡顿 / 失败 | 中 | T16 任务专门预留录屏时间，提前在同一台电脑上完整跑一次再录。 |
