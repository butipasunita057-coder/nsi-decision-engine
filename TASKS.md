# NSI 决策引擎 · 任务拆解清单（TASKS.md）

> 本文档是现场开发的"操作手册"，配合 SPEC.md（规格）与 PLAN.md（计划）使用。
> 每个任务均按统一模板列出：编号 / 名称 / 目标 / 依赖 / 涉及文件 / 本轮只做 / 不要做 / 验收标准 / 测试方式 / 是否现场执行 / 完成后输出。
> **注意**：T0 之前所有任务均不得开始。赛事开赛后再按顺序执行。

---

## 任务总览

| 编号 | 名称 | 现场执行 | 优先级 |
| --- | --- | --- | --- |
| T0 | 读取文档并确认范围 | 是 | P0 |
| T1 | 创建项目基础文件 | 是 | P0 |
| T2 | 创建本地 Demo 数据 | 是 | P0 |
| T3 | 实现数据读取 | 是 | P0 |
| T4 | 实现 ASIN 输入与示例选择 | 是 | P0 |
| T5 | 实现 NSI 计算函数 | 是 | P0 |
| T6 | 实现红黄绿分类 | 是 | P0 |
| T7 | 实现排序逻辑 | 是 | P0 |
| T8 | 实现整体健康度评分 | 是 | P0 |
| T9 | 实现结果概览区 | 是 | P0 |
| T10 | 实现三色卡片展示 | 是 | P0 |
| T11 | 实现问题详情展开区 | 是 | P1 |
| T12 | 实现错误提示 | 是 | P1 |
| T13 | 完善 README | 是 | P2 |
| T14 | 最终联调与自测 | 是 | P0 |
| T15 | 准备 GitHub 提交 | 是 | P0 |
| T16 | 准备 5 分钟录屏 | 是 | P0 |
| T17 | 准备 PPT 内容 | 是 | P0 |
| T18 | 输出执行总结 | 是 | P2 |

---

## T0 · 读取文档并确认范围

- **任务目标**：全队对齐 SPEC / PLAN / TASKS，明确"做什么、不做什么、谁干什么"。
- **依赖关系**：无。
- **涉及文件**：SPEC.md、PLAN.md、TASKS.md（只读）。
- **本轮只做**：
  1. 通读 SPEC.md 全部 18 节。
  2. 通读 PLAN.md 14 节，重点确认时间分配。
  3. 通读 TASKS.md，确认 T1~T18 的边界。
  4. 全员一句话复述：项目要做什么 / 不做什么。
  5. 现场分工：1 人主代码、1 人辅助代码 + 数据、1 人 PPT + 录屏 + 真实性证据。
- **不要做**：
  1. 不修改 SPEC / PLAN / TASKS。
  2. 不写任何代码。
  3. 不安装任何依赖。
- **验收标准**：所有队员能用一句话准确复述项目定位与禁止事项；分工记录在白板或便签上。
- **测试方式**：抽查任意一名队员，让其口头复述。
- **是否现场执行**：是。
- **完成后输出**：白板或纸面分工表（拍照保存）；可选地在 GitHub 仓库的 `docs/onsite-roles.md` 占位文件中写一行分工说明（可不创建）。

---

## T1 · 创建项目基础文件

- **任务目标**：搭建最小可运行 Streamlit 空骨架，确认环境可用。
- **依赖关系**：T0。
- **涉及文件**：`app.py`、`requirements.txt`。
- **本轮只做**：
  1. 在仓库根目录创建 `requirements.txt`，内容仅写 `streamlit`（如确实需要可加 `pandas`）。
  2. 创建 `app.py`，仅写：页面标题"NSI 决策引擎"、副标题"先改哪个，怎么改"、占位文字。
  3. 在现场电脑执行 `pip install -r requirements.txt`。
  4. 执行 `streamlit run app.py`，浏览器能打开页面。
- **不要做**：
  1. 不要在 `app.py` 中写任何业务逻辑（NSI、分类、排序）。
  2. 不要创建 `src/`、`backend/` 等子目录。
  3. 不要引入禁止清单中的依赖。
- **验收标准**：浏览器打开 `http://localhost:8501` 能看到标题与副标题，无报错。
- **测试方式**：本地运行 + 浏览器肉眼确认。
- **是否现场执行**：是。
- **完成后输出**：可运行的空骨架 + 第一次现场 commit（建议 message：`T1: bootstrap streamlit app`）。

---

## T2 · 创建本地 Demo 数据

- **任务目标**：构造演示用 `demo_data.json`，覆盖红 / 黄 / 绿三色与 P/L/S/M 归因。
- **依赖关系**：T1。
- **涉及文件**：`demo_data.json`。
- **本轮只做**：
  1. 按 SPEC 第 11 节字段结构创建 `demo_data.json`。
  2. 至少 2 个 ASIN（建议 ASIN：`B0EXAMPLE01`、`B0EXAMPLE02`）。
  3. 每个 ASIN 至少 6 个特征，覆盖：
     - 至少 1 个红色 + P 归因（如续航虚标）
     - 至少 1 个红色 + L 归因（如尺寸描述错）
     - 至少 1 个红色 + S 归因（如物流慢）
     - 至少 1 个 M 归因（恶意评论示例）
     - 至少 1 个黄色样例
     - 至少 1 个绿色样例
  4. 每个特征 `examples` 至少 1 条正面 + 1 条负面（M 归因可以只放负面）。
  5. 用 `python -m json.tool demo_data.json` 校验 JSON 合法。
- **不要做**：
  1. 不写 Python 脚本生成数据，直接手写。
  2. 不引入真实抓取数据。
  3. 不超过 3 个 ASIN，避免数据维护成本。
- **验收标准**：JSON 合法；红 / 黄 / 绿样例齐全；P / L / S / M 归因均有；每个特征字段无缺失。
- **测试方式**：`python -m json.tool demo_data.json` 无报错；人工肉眼复核字段完整性。
- **是否现场执行**：是。
- **完成后输出**：合法 `demo_data.json`；现场 commit（`T2: add demo_data.json with two example asins`）。

---

## T3 · 实现数据读取

- **任务目标**：在 `app.py` 中实现一个加载 `demo_data.json` 的函数，并在启动时缓存。
- **依赖关系**：T2。
- **涉及文件**：`app.py`。
- **本轮只做**：
  1. 编写 `load_data(path: str) -> dict`，读取 JSON 并返回字典。
  2. 用 `@st.cache_data` 装饰器缓存加载结果。
  3. 加载失败时返回空字典 `{}` 并在页面显示中文提示（暂时使用 `st.error`）。
- **不要做**：
  1. 不要在加载函数里做 NSI 计算或字段加工。
  2. 不要把 JSON 内容硬编码到 Python。
  3. 不要写文件写操作（这是只读工具）。
- **验收标准**：在页面上能用 `st.write(list(data.keys()))` 打印出所有 ASIN。
- **测试方式**：在 `app.py` 中临时打印 ASIN 列表，浏览器肉眼确认。
- **是否现场执行**：是。
- **完成后输出**：`app.py` 包含可用的 `load_data` 函数；commit（`T3: add data loader`）。

---

## T4 · 实现 ASIN 输入与示例选择

- **任务目标**：提供下拉选择 + 手动输入两种方式让用户指定 ASIN，并以"Analyze"按钮触发分析。
- **依赖关系**：T3。
- **涉及文件**：`app.py`。
- **本轮只做**：
  1. 用 `st.selectbox` 显示 demo_data.json 中的所有 ASIN。
  2. 用 `st.text_input` 提供手动输入框。
  3. 用 `st.button("Analyze")` 触发分析（点击后将选择 / 输入的 ASIN 存入会话状态）。
  4. 输入处统一 `strip().upper()`。
  5. 选择下拉 + 手动输入同时存在时，以"手动输入非空"为准。
- **不要做**：
  1. 不要在该任务做错误提示（留给 T12 统一做）。
  2. 不要解析非 ASIN 字符串、做正则校验（保持简单）。
- **验收标准**：能选择 / 输入 ASIN，并在点击 Analyze 后正确读到该 ASIN 字符串。
- **测试方式**：在按钮点击后用 `st.write` 临时打印当前 ASIN。
- **是否现场执行**：是。
- **完成后输出**：可用的 ASIN 输入区；commit（`T4: add asin input controls`）。

---

## T5 · 实现 NSI 计算函数

- **任务目标**：实现 SPEC 第 12 节定义的 NSI 计算纯函数。
- **依赖关系**：T1（与 T2~T4 并行可启动）。
- **涉及文件**：`app.py`。
- **本轮只做**：
  1. 定义函数：`def calc_nsi(positive: int, negative: int, total: int) -> float | None`。
  2. `total <= 0` 时返回 `None`。
  3. `positive`、`negative` 任意为负数时返回 `None`。
  4. 否则返回 `(positive - negative) / total`。
  5. 在文件顶部加注释，明确公式与边界。
- **不要做**：
  1. 不要在该函数内做颜色分类（颜色逻辑放 T6）。
  2. 不要做四舍五入（展示层再做格式化）。
  3. 不要引入 numpy / pandas 仅为做这个除法。
- **验收标准**：单元测试式手测全部通过：
  - `calc_nsi(10, 0, 10)` → `1.0`
  - `calc_nsi(0, 10, 10)` → `-1.0`
  - `calc_nsi(5, 5, 10)` → `0.0`
  - `calc_nsi(0, 0, 0)` → `None`
  - `calc_nsi(-1, 0, 5)` → `None`
- **测试方式**：在 `app.py` 末尾临时写几行 `st.write(calc_nsi(...))` 肉眼比对，验证后删除。
- **是否现场执行**：是。
- **完成后输出**：纯函数 `calc_nsi`；commit（`T5: implement nsi calculation`）。

---

## T6 · 实现红黄绿分类

- **任务目标**：实现 SPEC 第 13 节的颜色分类纯函数。
- **依赖关系**：T5。
- **涉及文件**：`app.py`。
- **本轮只做**：
  1. 定义常量：`RED_THRESHOLD = -0.5`、`YELLOW_THRESHOLD = 0.0`。
  2. 定义函数：`def classify_color(nsi: float | None) -> str`，返回 `"red"` / `"yellow"` / `"green"` / `"gray"`。
  3. 边界严格按 SPEC：`nsi < -0.5` → red；`-0.5 <= nsi < 0` → yellow；`nsi >= 0` → green；`nsi is None` → gray。
- **不要做**：
  1. 不要在 UI 代码内重复硬编码阈值。
  2. 不要支持自定义阈值（保持唯一权威）。
- **验收标准**：手测：
  - `classify_color(-0.8)` → `"red"`
  - `classify_color(-0.5)` → `"yellow"`
  - `classify_color(-0.1)` → `"yellow"`
  - `classify_color(0.0)` → `"green"`
  - `classify_color(0.5)` → `"green"`
  - `classify_color(None)` → `"gray"`
- **测试方式**：临时 `st.write` 打印各边界返回值，验证后删除。
- **是否现场执行**：是。
- **完成后输出**：纯函数 `classify_color`；commit（`T6: implement color classification`）。

---

## T7 · 实现排序逻辑

- **任务目标**：将某个 ASIN 下的特征按 NSI 升序排序（最差在前），`None` 排在最后。
- **依赖关系**：T5、T6。
- **涉及文件**：`app.py`。
- **本轮只做**：
  1. 定义函数：`def sort_features(features: list[dict]) -> list[dict]`。
  2. 给每个特征计算 NSI（调用 `calc_nsi`）和颜色（调用 `classify_color`），写回字段（如 `_nsi` 与 `_color`）。
  3. 排序规则：NSI 升序；NSI 为 `None` 排在最后；同 NSI 时按 `feature_name` 字典序。
- **不要做**：
  1. 不要修改原始 `features` 列表（返回新列表）。
  2. 不要把排序写在 UI 渲染处（必须封装为函数）。
- **验收标准**：构造一组测试特征，排序结果与预期一致；红色样例排在最前；提及不足样例排在最后。
- **测试方式**：临时 `st.write` 打印排序结果列表的 `feature_name + _nsi + _color`，肉眼比对。
- **是否现场执行**：是。
- **完成后输出**：纯函数 `sort_features`；commit（`T7: implement feature sorting`）。

---

## T8 · 实现整体健康度评分

- **任务目标**：基于所有特征 NSI 计算 0~100 的整体健康度评分。
- **依赖关系**：T5。
- **涉及文件**：`app.py`。
- **本轮只做**：
  1. 定义函数：`def health_score(features: list[dict]) -> int`。
  2. 算法：取所有 `_nsi` 不为 `None` 的特征，计算其 NSI 平均值 `avg`（值域 [-1, 1]）；映射到 0~100：`score = round((avg + 1) * 50)`。
  3. 当所有特征 NSI 均为 `None` 时返回 `0`，并在调用层显示"无足够数据"提示。
  4. 在调用层根据分数给出文字结论：≥75 健康、50–74 一般、<50 危险。
- **不要做**：
  1. 不要引入复杂权重（保持平均值即可）。
  2. 不要把"健康 / 一般 / 危险"文案写死在评分函数内。
- **验收标准**：手测：
  - 全部 NSI = 1 → 100；全部 NSI = -1 → 0；全部 NSI = 0 → 50。
  - 部分 None 时仅基于有效项计算。
- **测试方式**：构造三组特征手测；临时 `st.write` 打印分数。
- **是否现场执行**：是。
- **完成后输出**：纯函数 `health_score`；commit（`T8: implement health score`）。

---

## T9 · 实现结果概览区

- **任务目标**：在页面顶部展示当前 ASIN 的标题、类目、简介、整体健康度评分、红黄绿数量统计。
- **依赖关系**：T3、T4、T7、T8。
- **涉及文件**：`app.py`。
- **本轮只做**：
  1. 用户点击 Analyze 后，根据 ASIN 取出对应数据。
  2. 渲染 `title` / `category` / `summary`。
  3. 用 `st.metric` 展示整体健康度评分及结论文字。
  4. 用 3 个并排的指标展示红 / 黄 / 绿特征数量。
- **不要做**：
  1. 不要在该任务渲染卡片明细（卡片在 T10 做）。
  2. 不要做样式美化、CSS 注入。
- **验收标准**：切换不同 ASIN，概览区数字立即更新；分数与红黄绿数量与 SPEC 阈值一致。
- **测试方式**：手动切换 ASIN，肉眼对比。
- **是否现场执行**：是。
- **完成后输出**：可用的概览区；commit（`T9: add overview section`）。

---

## T10 · 实现三色卡片展示

- **任务目标**：将排序后的特征以三色卡片形式自上而下展示。
- **依赖关系**：T7、T9。
- **涉及文件**：`app.py`。
- **本轮只做**：
  1. 遍历排序后的 features，对每个特征渲染一张卡片。
  2. 卡片内容：颜色条（emoji 🔴🟡🟢⚪ 即可）、特征名、NSI（保留 2 位小数）、正/负/总提及数、P/L/S/M 标签。
  3. 卡片顺序严格按 T7 排序结果。
  4. 颜色样式可用 `st.container` + emoji 实现，不引入复杂 CSS。
- **不要做**：
  1. 不要在卡片内嵌入复杂图表。
  2. 不要把展开详情塞到卡片里（详情在 T11 做）。
- **验收标准**：所有特征均显示一张卡片，颜色与 NSI 一致；P/L/S/M 标签无空。
- **测试方式**：肉眼浏览页面，对照 demo_data.json 校验。
- **是否现场执行**：是。
- **完成后输出**：可用的三色卡片列表；commit（`T10: render colored feature cards`）。

---

## T11 · 实现问题详情展开区

- **任务目标**：每张卡片支持展开，显示典型评论、诊断、建议动作、Listing 改写建议。
- **依赖关系**：T10。
- **涉及文件**：`app.py`。
- **本轮只做**：
  1. 用 `st.expander("查看详情")` 包裹详情区。
  2. 详情包含：
     - 典型评论列表（区分正面 / 负面用 emoji ✅ / ❌）。
     - `diagnosis` 一句话诊断。
     - `action` 建议动作。
     - `listing_rewrite` 改写建议。
  3. 缺字段时显示"暂无"占位，不抛错。
- **不要做**：
  1. 不要做评论分页 / 翻页（现场数据量小）。
  2. 不要调用 LLM 现场生成详情（如确实使用 LLM，必须有本地兜底）。
- **验收标准**：展开任一卡片能看到完整四块内容；缺字段卡片可正常展开并显示"暂无"。
- **测试方式**：人工展开每一张卡片肉眼确认。
- **是否现场执行**：是。
- **完成后输出**：可用的展开详情；commit（`T11: add expandable details`）。

---

## T12 · 实现错误提示

- **任务目标**：覆盖所有可预见的异常输入，给出中文友好提示，不让异常抛到页面。
- **依赖关系**：T4、T9、T10、T11。
- **涉及文件**：`app.py`。
- **本轮只做**：
  1. ASIN 不存在：`st.warning("未找到该 ASIN，请尝试下拉选择示例 ASIN。")`。
  2. demo_data.json 加载失败：`st.error("数据加载失败，请检查 demo_data.json 是否存在且格式合法。")`。
  3. 某个特征字段缺失：UI 显示"暂无"，不抛栈。
  4. 用户未输入 ASIN 直接点 Analyze：`st.info("请先选择或输入一个 ASIN，再点击 Analyze。")`。
  5. 整体健康度无法计算（全部 NSI 为 None）：`st.info("当前 ASIN 评论提及不足，无法计算健康度。")`。
- **不要做**：
  1. 不要用裸 `try/except` 吞掉所有异常（必须分类处理）。
  2. 不要弹原生 Python 错误堆栈给用户。
- **验收标准**：手动构造每种错误场景，UI 都给出对应中文提示，且页面其它区域不崩溃。
- **测试方式**：依次手测 5 种错误场景。
- **是否现场执行**：是。
- **完成后输出**：覆盖错误提示的代码；commit（`T12: add friendly error handling`）。

---

## T13 · 完善 README

- **任务目标**：写一份评委一眼能懂的 README.md，包含介绍、运行命令、Demo 截图、文档链接。
- **依赖关系**：T9、T10、T11。
- **涉及文件**：`README.md`。
- **本轮只做**：
  1. 顶部一句话介绍：项目名 + 一句话定位。
  2. 一段背景：卖家面对评论不知先改哪个的真实痛点。
  3. 运行命令：
     ```
     pip install -r requirements.txt
     streamlit run app.py
     ```
  4. 主要功能列表（不超过 6 条）。
  5. Demo 截图（至少 1 张概览 + 1 张卡片详情）。
  6. 链接到 SPEC.md / PLAN.md / TASKS.md。
  7. 项目真实性声明（一段话）：核心代码均现场完成，赛前仅准备文档。
- **不要做**：
  1. 不写超长背景介绍 / 行业分析。
  2. 不写 API 文档（项目不对外提供 API）。
  3. 不写 Roadmap、Contributing 等无关章节。
- **验收标准**：陌生人读完 README 能在 3 分钟内跑通项目。
- **测试方式**：找一名队员（非负责 README 的人）按照 README 步骤走一遍，是否能跑通。
- **是否现场执行**：是。
- **完成后输出**：完整 README.md；commit（`T13: complete README`）。

---

## T14 · 最终联调与自测

- **任务目标**：从头到尾走一遍 SPEC 第 16 节演示路径，修复一切阻塞 bug。
- **依赖关系**：T1~T13。
- **涉及文件**：`app.py`、`demo_data.json`、`README.md`。
- **本轮只做**：
  1. 关掉所有 IDE 调试窗口，只保留浏览器 + 终端。
  2. 清空 Streamlit 缓存（`Rerun` / 重启）。
  3. 按 SPEC 第 16 节 10 步演示路径完整跑一遍。
  4. 切换至少 2 个不同 ASIN，确认结果不串。
  5. 故意输错一次 ASIN，确认错误提示正常。
  6. 修复所有阻塞 bug；非阻塞美化问题暂不处理。
- **不要做**：
  1. 不要在该任务做样式重构。
  2. 不要在该任务新增功能。
- **验收标准**：演示路径全程无报错、无空白页面、无字段乱码。
- **测试方式**：由 PPT / 路演同学走一遍，作为评委视角自测。
- **是否现场执行**：是。
- **完成后输出**：通过自测的最终版 `app.py`；commit（`T14: integration self test fixes`）。

---

## T15 · 准备 GitHub 提交

- **任务目标**：保证 GitHub 仓库公开、最新、可访问、可作为评委查看入口。
- **依赖关系**：T14。
- **涉及文件**：GitHub 仓库（远端）。
- **本轮只做**：
  1. 确认仓库为 Public。
  2. 确认 `app.py` / `demo_data.json` / `requirements.txt` / `README.md` / `SPEC.md` / `PLAN.md` / `TASKS.md` 全部已推送。
  3. 推送所有未提交的本地变更，避免遗漏。
  4. 打 tag：`git tag v1.0-demo` 并 push tag。
  5. 截图保存：commit 时间线 + tag 列表，作为真实性证据备查。
- **不要做**：
  1. 不要 force push、不要 squash 历史。
  2. 不要在仓库中放敏感信息（API Key 等）。
- **验收标准**：用无痕浏览器打开仓库 URL，能看到全部文件 + commit 时间线 + v1.0-demo tag。
- **测试方式**：手机或他人电脑访问仓库 URL。
- **是否现场执行**：是。
- **完成后输出**：仓库 URL + commit 时间线截图 + tag 截图。

---

## T16 · 准备 5 分钟录屏

- **任务目标**：录制不超过 5 分钟的 Demo 录屏，覆盖完整演示路径。
- **依赖关系**：T14。
- **涉及文件**：录屏文件（mp4）。
- **本轮只做**：
  1. 在同一台电脑上跑通完整演示路径 ≥1 次。
  2. 用录屏工具（OBS / 系统自带）录制屏幕 + 麦克风。
  3. 按 PLAN 第 11 节的结构录制（痛点→定位→Demo→价值→真实性声明）。
  4. 控制总长 ≤ 5 分钟。
  5. 导出 mp4，文件名 `nsi-decision-engine-demo.mp4`。
  6. 备份至少 1 份到 U 盘 / 网盘。
- **不要做**：
  1. 不做复杂剪辑、转场、配乐。
  2. 不录冗长的"项目背景介绍"。
  3. 不在录屏中暴露任何敏感信息。
- **验收标准**：mp4 时长 ≤ 5 分钟，画面清晰、声音可辨、流程完整。
- **测试方式**：用另一台电脑播放录屏，确认能看清屏幕文字。
- **是否现场执行**：是。
- **完成后输出**：`nsi-decision-engine-demo.mp4`。

---

## T17 · 准备 PPT 内容

- **任务目标**：完成不超过 12 页的 PPT，覆盖痛点、方案、Demo、价值、真实性声明。
- **依赖关系**：T14、T15。
- **涉及文件**：PPT（pptx + pdf）。
- **本轮只做**：
  1. 按 PLAN 第 12 节的结构起稿 8~10 页。
  2. Demo 截图必须取自 T14 自测后的真实页面。
  3. 关键页加 NSI 公式 + 红黄绿阈值 + P/L/S/M 解释。
  4. 真实性声明页：贴 GitHub commit 时间线截图（来自 T15）。
  5. 导出 pptx + pdf 双格式备份。
- **不要做**：
  1. 不写超过 12 页。
  2. 不用花哨模板，使用极简白底深字。
  3. 不写无关的行业大盘数据。
- **验收标准**：5 分钟内能讲完；评委一眼能 get 项目定位与差异化。
- **测试方式**：路演同学限时 5 分钟试讲一次，全队复盘。
- **是否现场执行**：是。
- **完成后输出**：`NSI-Decision-Engine.pptx` + `NSI-Decision-Engine.pdf`。

---

## T18 · 输出执行总结

- **任务目标**：向团队 + 评委（必要时）输出本次现场开发的简要总结，证明真实性 + 完成度。
- **依赖关系**：T15、T16、T17。
- **涉及文件**：可选地新增 `docs/onsite-summary.md`（不强制）。
- **本轮只做**：
  1. 用 5~8 句话写清：
     - 现场用了多长时间。
     - 完成了哪些 T 任务、未完成哪些。
     - GitHub 仓库 URL。
     - 录屏 + PPT 路径。
     - 真实性证据：commit 时间线 / 文档先于代码。
  2. 复盘：哪些任务超时，原因是什么。
- **不要做**：
  1. 不写长篇博客。
  2. 不复制 SPEC / PLAN 内容。
- **验收标准**：总结一页可读完；信息完整可作为路演备份口径。
- **测试方式**：让队内非作者一人朗读，确认无歧义。
- **是否现场执行**：是。
- **完成后输出**：执行总结文本（口头 / 文档形式皆可）。

---

## 现场使用说明

1. **顺序执行**：T0 → T18，原则上不跳跃。可在 T5、T6、T8 之间并行（不同人各自负责），其它任务尽量串行。
2. **任务粒度提交**：每个任务完成立即 commit，message 以编号开头（如 `T7: implement feature sorting`）。
3. **遇阻**：若某任务卡 30 分钟未推进，立刻按 PLAN 第 8 节的优先级砍掉非 P0 部分，先保障主路径。
4. **AI 编码助手**：可在每个任务内使用 Claude Code / Codex / Qoder，但 prompt 必须由队员主导，AI 输出必须经队员逐行 review 后才合入。
5. **文档不动**：现场不修改 SPEC / PLAN / TASKS（除非发现明显错误，需全队同意后再改并 commit）。
