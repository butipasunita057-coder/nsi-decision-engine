# NSI 决策引擎 · 开发计划（PLAN.md）

> 本文档配合 SPEC.md 与 TASKS.md 使用，回答"现场两天怎么干、按什么顺序、按什么纪律交付"。
> 适用人群：现场队员、AI 编程助手（Claude Code / Codex / Qoder）、产品 / 路演同学。

---

## 1. 当前阶段判断

**当前阶段：赛前文档准备阶段（Pre-Hackathon, Documentation Only）**

判断依据：
- 黑客松将"项目真实性"列为一票否决项，要求核心代码、核心逻辑、核心创意必须现场完成。
- 我们尚未到达赛事正式开始时间。
- 因此，本阶段唯一允许的产出：SPEC.md / PLAN.md / TASKS.md 三份文档。

**严格禁止在当前阶段做的事**：
- 不写 `app.py`。
- 不写 `demo_data.json`。
- 不写 `requirements.txt`。
- 不写 `README.md`。
- 不创建任何 Python 源文件。
- 不在本仓库提交任何可运行代码。
- 不预先安装项目依赖到一份"准好的"环境。
- 不预先做 PPT 或录屏（仅可起草大纲，不做最终成品）。

**进入下一阶段的条件**：
- 赛事正式开始（按比赛手册公布的 Day1 9:00 开赛时间起算）。
- 队员到场、签到完成、获得参赛证。
- 此时才允许按 TASKS.md 从 T0 开始执行代码开发。

---

## 2. 总开发原则

1. **小而真**：项目永远只解决"卖家不知道先改哪个"这一个问题。任何与之无关的扩展请求一律拒绝。
2. **闭环优先**：先跑通"输入 ASIN → 出三色清单"主路径，再优化 UI、文案、动效。
3. **数据驱动 UI**：UI 只读 demo_data.json，不在 UI 内做复杂数据加工。
4. **纯函数优先**：NSI 计算、分类、排序全部写成纯函数，便于现场快速自测。
5. **错误兜底**：任何外部输入（ASIN 输入框、JSON 字段缺失）必须有友好兜底，不让异常抛到页面。
6. **每步可演示**：每完成一个 T 任务都能在浏览器看到肉眼可见的进展，便于评委信任开发过程真实性。
7. **每个任务一次提交**：commit message 形如 `T5: implement nsi calculation`，commit 时间线即开发证据。
8. **不偏离技术栈**：不引入 SPEC 第 8 节禁止的任何依赖。
9. **不堆代码**：宁少不多。能 50 行解决的事，不写 200 行。
10. **不写测试框架**：现场使用人工 + Streamlit 页面自测即可，不引入 pytest 等。

---

## 3. 技术栈固定说明

| 类别 | 选择 | 备注 |
| --- | --- | --- |
| 语言 | Python 3.10+ | 现场环境如无 3.10，使用现场提供的最新版即可 |
| UI | Streamlit (≥1.30) | 唯一前端方案 |
| 数据 | 本地 JSON 文件 `demo_data.json` | 不接数据库 |
| 计算 | Python 标准库 | 必要时使用 `pandas` |
| LLM（可选，非关键） | OpenAI 兼容接口 / 通义千问 | 仅用于 Listing 改写文案的现场增强；必须有"无 API Key 时也能跑通"的本地兜底 |
| 版本控制 | Git + GitHub | 仓库必须公开 |
| 运行命令 | `streamlit run app.py` | 唯一启动方式 |

**禁止引入**（重申）：FastAPI、Flask、Django、React、Vue、Docker、PostgreSQL、MySQL、SQLite、MongoDB、Redis、Selenium、Playwright、Scrapy、LangChain、任何鉴权框架。

---

## 4. 目标项目结构（现场最终结构）

```
nsi-decision-engine/
├── app.py                  # 主入口（T1 创建，T3~T12 持续完善）
├── demo_data.json          # 本地示例数据（T2 创建）
├── requirements.txt        # 依赖清单（T1 创建）
├── README.md               # 项目说明（T13 完善）
├── SPEC.md                 # 项目规格说明（赛前已存在）
├── PLAN.md                 # 开发计划（赛前已存在）
└── TASKS.md                # 任务拆解（赛前已存在）
```

**附加约束**：
- 不创建 `src/`、`backend/`、`frontend/`、`tests/` 等多层目录。
- 不创建 `.env`、`config.yaml` 等配置文件（必要常量直接写在 `app.py` 顶部）。
- 不引入 `__init__.py` 等包结构。
- 项目结构变更需要全队同意，且不得引入新增依赖。

---

## 5. 分阶段开发计划

将赛事 Day1 + Day2 划分为 6 个阶段。每个阶段对应 TASKS.md 中若干 T 任务。

### 阶段 P0 · 启动与确认（Day1 上午前 1 小时）

- 涉及任务：T0
- 目标：现场签到完成后，全队对齐 SPEC / PLAN / TASKS，确认本日仅做 MVP 主路径。
- 涉及文件：SPEC.md、PLAN.md、TASKS.md（只读）。
- 验收标准：全员能用一句话说清"项目要做什么 + 不做什么"。

### 阶段 P1 · 项目骨架与数据（Day1 上午）

- 涉及任务：T1、T2
- 目标：创建可运行的最小空骨架 + 本地示例数据。
- 涉及文件：`app.py`、`requirements.txt`、`demo_data.json`。
- 验收标准：
  1. `streamlit run app.py` 能弹出空白页（含项目标题）。
  2. `demo_data.json` 包含至少 2 个 ASIN，每个 ASIN 至少 6 个特征，含红/黄/绿各类样例。

### 阶段 P2 · 核心算法（Day1 下午）

- 涉及任务：T3、T4、T5、T6、T7、T8
- 目标：现场完成"读 JSON → ASIN 输入 → NSI 计算 → 三色分类 → 排序 → 整体健康度评分"全部纯逻辑。
- 涉及文件：`app.py`、`demo_data.json`。
- 验收标准：
  1. 在 Streamlit 页面输入 / 选择 ASIN，点击 Analyze 后能在终端 / 页面打印或展示出排序后的特征列表。
  2. 红黄绿分类与 NSI 数值严格匹配 SPEC 第 13 节。
  3. 整体健康度评分可正常输出 0~100 整数。

### 阶段 P3 · UI 闭环（Day1 晚 / Day2 上午）

- 涉及任务：T9、T10、T11、T12
- 目标：把 P2 的算法接到 UI，形成完整可演示页面。
- 涉及文件：`app.py`。
- 验收标准：
  1. 概览区、三色卡片、详情展开、错误提示全部正常。
  2. 切换不同 ASIN 后界面立即刷新。
  3. 输入不存在的 ASIN 显示中文友好提示，不抛栈。

### 阶段 P4 · 收尾与自测（Day2 中午）

- 涉及任务：T13、T14
- 目标：完善 README，自测 MVP 演示路径，修补小 bug。
- 涉及文件：`app.py`、`demo_data.json`、`README.md`。
- 验收标准：
  1. README 包含一句话简介、运行命令、截图占位、SPEC/PLAN/TASKS 链接。
  2. 完整跑通 SPEC 第 16 节的 10 步演示路径，无报错。

### 阶段 P5 · 提交物（Day2 下午，路演前）

- 涉及任务：T15、T16、T17、T18
- 目标：完成 GitHub、5 分钟录屏、PPT、最终执行总结。
- 涉及文件：GitHub 仓库、录屏文件、PPT 文件、总结说明。
- 验收标准：
  1. GitHub 仓库公开，README 可见，可被评委一键访问。
  2. 录屏不超过 5 分钟，覆盖 SPEC 第 16 节演示路径。
  3. PPT 不超过 12 页，覆盖痛点 / 方案 / Demo / 价值 / 真实性声明。
  4. 执行总结说明全队角色分工与 commit 时间线。

---

## 6. 各阶段目标 / 文件 / 验收一览（汇总表）

| 阶段 | 时间窗 | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- | --- |
| P0 | Day1 09:00–10:00 | T0 | SPEC/PLAN/TASKS | 全员对齐范围 |
| P1 | Day1 10:00–12:00 | T1, T2 | app.py / requirements.txt / demo_data.json | 空骨架 + 样例数据可加载 |
| P2 | Day1 13:00–18:00 | T3–T8 | app.py / demo_data.json | 算法可用，能排序输出 |
| P3 | Day1 19:00 – Day2 11:00 | T9–T12 | app.py | UI 闭环、错误兜底 |
| P4 | Day2 11:00–13:00 | T13, T14 | app.py / demo_data.json / README.md | 演示路径全跑通 |
| P5 | Day2 13:00 – 路演前 | T15–T18 | GitHub / 录屏 / PPT | 提交物齐全 |

---

## 7. 现场开发真实性策略

为兑现"项目真实性"一票否决项，现场严格执行：

1. **先文档后代码**：到场后第一件事是 `git pull` 现有 SPEC/PLAN/TASKS，再根据 TASKS 顺序开发。
2. **任务级提交**：每个 T 任务至少一个 commit，commit message 以任务编号开头（如 `T5: implement nsi calculation`）。
3. **小步快跑**：禁止"先在本地写完再一次性提交"；提倡 30~60 分钟一个 commit。
4. **保留中间产物**：保留中间报错截图、调试痕迹（如临时打印），更显真实。
5. **演示时主动声明**：路演开场用 1 句话说明"本项目核心代码均现场完成，赛前仅准备 SPEC / PLAN / TASKS 文档"。
6. **可证据化**：随时准备好 GitHub commit 时间线截图，作为真实性辅证。
7. **禁止粘贴大段陈年代码**：所有现场写的代码必须是当场理解、当场打字。
8. **AI 编码工具仅辅助，不代写核心创意**：使用 Claude Code / Codex / Qoder 时，由队员主导 prompt，AI 仅提示语法或样板代码。

---

## 8. 时间优先级

按照"如果时间不够，先砍什么"的顺序排序：

| 优先级 | 内容 | 不能砍的原因 |
| --- | --- | --- |
| P0（绝不能砍） | T0–T8（数据 + 核心算法） | 没有这些，Demo 跑不动，AI 链路不存在 |
| P0（绝不能砍） | T9、T10（概览 + 三色卡片） | 没有这些，无法演示 |
| P0（绝不能砍） | T15、T16、T17（GitHub / 录屏 / PPT） | 这是提交硬要求 |
| P1（尽量做） | T11（详情展开） | 没有它，故事不完整，但仍可演 |
| P1（尽量做） | T12（友好错误） | 没有它，演示翻车风险大 |
| P2（可砍） | T13（README 完善） | 可只写最小说明 |
| P2（可砍） | T14（联调自测）的美化部分 | 必须保留主路径自测，可砍样式美化 |
| P2（可砍） | T18（执行总结） | 时间紧时压成 1 段话 |

---

## 9. 评分维度对应策略

| 评分维度 | 权重 | 操作动作 |
| --- | --- | --- |
| 技术实现（40 分） | 40% | 用 SPEC 第 12~14 节的清晰公式 + 明确分类 + P/L/S/M 归因，证明 AI 真正参与核心流程；演示中口头讲一遍 NSI 公式与红黄绿阈值。 |
| 创意 / 商业价值（30 分） | 30% | PPT 第 1 页用一句话锚定痛点；用"先改哪个 / 怎么改"差异化定位；说明面向亚马逊中小卖家的商业落点。 |
| 完成度（30 分） | 30% | 演示全程不报错；GitHub README、PPT、5 分钟录屏齐全；演示路径与 SPEC 第 16 节完全一致。 |
| 项目真实性（一票否决） | 否决 | 严格执行第 7 节策略；GitHub commit 时间线作为证据；现场开发不复制赛前代码。 |

---

## 10. GitHub 提交策略

1. **仓库可见性**：现场创建公开仓库（Public），命名建议 `nsi-decision-engine`。
2. **首个 commit**：到场签到完成后，将 SPEC.md / PLAN.md / TASKS.md 作为第一次 commit 推上去；commit message：`docs: initial spec/plan/tasks`。
3. **逐 T 提交**：之后每完成一个 T 任务，立即提交，commit message 以任务编号开头，例如：
   - `T1: bootstrap streamlit app`
   - `T2: add demo_data.json with two example asins`
   - `T5: implement nsi calculation`
4. **禁止 force push**、**禁止 squash 历史**：保留真实开发轨迹。
5. **README**：T13 完成后推送一次最终版本，包含截图、运行命令、SPEC/PLAN/TASKS 链接。
6. **最终 tag**：路演前 30 分钟打一个 tag `v1.0-demo`，便于评委定位提交版本。

---

## 11. 5 分钟录屏准备策略

1. **录屏长度**：≤ 5 分钟，建议 3 分 30 秒到 4 分 30 秒之间。
2. **结构**：
   - 0:00–0:30 痛点引入
   - 0:30–1:00 项目定位（NSI 决策引擎，先改哪个怎么改）
   - 1:00–3:30 Demo 操作（按 SPEC 第 16 节 10 步演示路径）
   - 3:30–4:30 价值与差异化（NSI + 三色 + P/L/S/M 归因）
   - 4:30–5:00 真实性声明 + 团队致谢
3. **录制工具**：现场使用 OBS / 系统自带录屏 / QuickTime 任一即可，不引入复杂依赖。
4. **录制顺序**：先在同一台电脑跑通完整演示路径 ≥1 次，再开始录屏。
5. **音频**：现场环境嘈杂时使用降噪麦克风或后期配音。
6. **导出**：导出 mp4，文件名 `nsi-decision-engine-demo.mp4`，与 PPT 同目录提交。

---

## 12. PPT 准备策略

1. **页数**：≤ 12 页，目标 8~10 页。
2. **结构（推荐）**：
   1. 封面：项目名 / 一句话定位 / 团队
   2. 痛点：卖家面对评论不知先改哪个
   3. 方案：NSI 决策引擎一句话说明
   4. 核心方法：NSI 公式 + 红黄绿阈值
   5. 差异化：P/L/S/M 归因分类
   6. Demo 截图（概览 + 三色清单）
   7. Demo 截图（卡片详情展开）
   8. 商业价值：服务亚马逊中小卖家
   9. 技术链路 + AI 在其中的位置
   10. 项目真实性声明（仅赛前文档，核心代码现场完成，附 GitHub commit 时间线截图）
   11. 团队介绍
   12. Q&A / 谢谢
3. **风格**：极简风、白底、深色字、关键词突出。
4. **截图来源**：必须是现场 Demo 真实截图，不允许使用赛前虚构 UI。

---

## 13. 最终提交物检查清单

路演开始前必须确认以下全部为"是"：

- [ ] GitHub 仓库公开可访问
- [ ] 仓库根目录包含 `app.py` / `demo_data.json` / `requirements.txt` / `README.md` / `SPEC.md` / `PLAN.md` / `TASKS.md`
- [ ] `streamlit run app.py` 在赛事电脑上可一键启动
- [ ] 演示路径与 SPEC 第 16 节一致，无报错
- [ ] 5 分钟录屏 mp4 已导出
- [ ] PPT 已导出 pdf 和 pptx 两份
- [ ] PPT 含项目真实性声明 + GitHub commit 时间线截图
- [ ] 团队成员全员到场并完成签到
- [ ] commit 时间线连续，无大段空白与一次性大提交
- [ ] README 含运行命令、截图、SPEC/PLAN/TASKS 链接
- [ ] 录屏与 PPT 提交方式（评委指定的链接 / 群文件）已就位

---

## 14. 风险清单

| 风险 | 等级 | 触发条件 | 应对动作 |
| --- | --- | --- | --- |
| 现场无法安装 Streamlit | 高 | pip install 报错 | 切换镜像源（清华 / 阿里）或申请使用赛事提供的预装环境 |
| Streamlit 启动慢 / 端口冲突 | 中 | `streamlit run` 卡住或报端口占用 | 使用 `--server.port 8502` 切换端口 |
| demo_data.json 写错格式 | 高 | UI 报 JSON 解析错误 | T2 任务结束前必须用 `python -m json.tool demo_data.json` 校验 |
| ASIN 大小写 / 空格问题 | 中 | 用户输入带空格被判为不存在 | 输入处统一 `strip().upper()` |
| LLM API 现场失败 | 中 | 网络受限 / Key 失效 | 默认走本地兜底文案，不阻塞主路径 |
| 时间超支 | 高 | P2 阶段超过 18:00 仍未完成 | 立即停止 T 任务推进，跳过 P1 优先级，确保 P0 任务先完成 |
| 评委质疑真实性 | 极高 | 评委明确询问 | 现场打开 GitHub 提交时间线 + 文档 commit 在前的截图 |
| 录屏时 Demo 翻车 | 中 | 录屏中报错 | 提前在同台电脑跑通 ≥1 次；保留一份录屏备份 |
| Git 推送失败 | 中 | 网络不稳定 | 备用方案：用手机热点 / 提前注册的镜像账号 |
| 团队分工冲突 | 中 | 同一文件多人改 | 每个 T 任务指定一个负责人；其他人改文档或准备 PPT |
| 误引入禁止依赖 | 高 | 有人临时 `pip install fastapi` | 全队默认拒绝任何不在 requirements.txt 计划中的依赖 |
| AI 工具生成大段无关代码 | 中 | 现场粘贴 AI 输出 | 强制规则：所有 AI 生成代码必须经队员逐行 review，与 SPEC/TASKS 不符的必须删除 |
